// Shared types, schemas, atoms, and utils for Strava Overlay Studio.
// Populated incrementally — Phase 3 adds overlay types, Phase 4 adds Strava schemas
// and session atoms, Phase 5 adds format helpers. Kept intentionally empty in Phase 1
// so the three apps can already import this workspace package.
export {};
